Sources for the plugin `CoronaProvider.gameNetwork.apple`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "CoronaProvider.gameNetwork.apple" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
